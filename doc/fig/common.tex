\documentclass{standalone}

\usepackage{tikz}
\usetikzlibrary{trees}
\begin{document}
\tikzstyle{every node}=[draw=black,thick,anchor=west]
\tikzstyle{selected}=[draw=green,fill=green!30]
\tikzstyle{optional}=[dashed,fill=gray!20]
\begin{tikzpicture}[%
  grow via three points={one child at (0.5,-0.7) and
  two children at (0.5,-0.7) and (0.5,-1.4)},
  edge from parent path={(\tikzparentnode.south) |- (\tikzchildnode.west)}]
  \node {common}
    child { node {contig\_alloc}}		
    child { node {esp}}
	child { node {include}}
	child { node {probe}}
	child { node [selected] {lib-math}}
	child { node [selected] {mnemosyne-tcl}}
	child { node {scripts}
      	child { node {bin}
	      	child { node {ctos}}
    		child { node {dc}}
      		child { node {vivado}}
      		child { node {quartus}}
      	}		
		child [missing] {}
		child [missing] {}
		child [missing] {}
		child [missing] {}
	    child { node {ctos\_scripts}}
		child { node {dc\_scripts}}
		child { node {vivado\_scripts}}
		child { node {quartus\_scripts}}
		}
	child [missing] {}
	child [missing] {}
	child [missing] {}
	child [missing] {}
	child [missing] {}
	child [missing] {}
	child [missing] {}
	child [missing] {}
	child [missing] {}
	child { node {tech}}
    ;
\end{tikzpicture}

\end{document}
